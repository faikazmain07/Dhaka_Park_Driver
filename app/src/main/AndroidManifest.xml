<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required for Google Maps, location, and internet access. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DhakaParkDriver"
        tools:targetApi="31">

        <!-- Secured Google Maps API key. This is a best practice. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!--
         LAUNCHER ACTIVITY
         MainActivity is now the single entry point for the app.
         It hosts the NavHostFragment which controls all the fragment-based UI.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
         REGULAR ACTIVITIES
         These are other full-screen activities in the app that are launched via Intents.
         They are not entry points from the phone's home screen.
        -->
        <activity
            android:name=".DriverDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".BookingActivity"
            android:exported="false" />

        <!--
         NOTE: Old activities like SplashActivity and LoginActivity have been removed
         because their functionality is now handled by Fragments inside MainActivity.
         We will create and add RegisterActivity/Fragment later.
        -->

    </application>

</manifest>