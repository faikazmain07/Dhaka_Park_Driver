<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
    Permissions required for Google Maps.
    FINE_LOCATION is for high-precision GPS.
    COARSE_LOCATION is for network-based location.
    INTERNET is required to download map tiles.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DhakaParkDriver"
        tools:targetApi="31">

        <!--
        !!!! CRITICAL !!!!
        You MUST get a Google Maps API key and paste it here.
        The map will NOT work without it.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDVJlPCPx7zFkiPBJYT0dKm7Fe2BZ0NJuM" />


        <!-- Your Activities -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MapActivity" />
        <!-- Add other activities like WelcomeActivity here -->

    </application>

</manifest>